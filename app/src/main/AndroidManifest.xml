<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.b8a3.interview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Interview">
        <activity android:name="com.b8a3.interview.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AActivity"
            />
        <activity android:name=".activity.BActivity"
            android:theme="@style/Theme.AppCompat.Dialog"/>
        <activity
            android:name=".activity.XActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".activity.YActivity"
            android:launchMode="singleInstance" />
        <activity android:name=".activity.ViewVisibilityActivity" />
    </application>

</manifest>